# Server configuration
# Copy this file to ".env" and adjust values as needed.
# dotenv is configured with override=true, so these values
# will replace existing environment variables of the same name.

# Port number the Express API should listen on.
PORT=4000
